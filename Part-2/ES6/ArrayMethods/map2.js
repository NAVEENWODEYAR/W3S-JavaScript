// ES-6 Array methods- map(), foreach(),reduce(),filter().

   let ids = [1,2,3,4,5];
      ids.map((a)=>{return ids.id});

      ids.forEach((id)=>
         {
            console.log(id+2);
         })
         