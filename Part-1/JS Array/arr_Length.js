// length is a property in JavaScript -  used to check the array size & set the array size.

let employee = [1,2,3,4,5,6,7,8,9];
console.log(employee.length);

let student = ["A","B"]
console.log(student.length);            // 2
student.length = 10;
console.log(student.length);            // 10
