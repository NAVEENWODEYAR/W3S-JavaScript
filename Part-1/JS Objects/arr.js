// JavaScript Array

let arr = [1,2,3,4,5,6];
/*console.log(typeof (arr));
console.log(arr.length);

    console.log("*** Iterating the array ***");
    for (a in arr)  
    {
        console.log(arr[a]);
    }
*/
    console.log(arr.push(7,8,9,10));                     // appends the new elements to the array at the end.
    console.log(arr);

    console.log(arr.reverse());                         // reverse()

    console.log(arr.toString());                        // toString()

    for (a of arr)
    {
        console.log(a);
    }


    
     